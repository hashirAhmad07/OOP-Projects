Split Temporary Variable: I ensured that each variable has a single responsibility.

Remove Assignments to Parameters: I did not find any assignments to parameters that needed removal in the given code.

Replace Method with Method Object: I kept the methods simple and focused on a single responsibility.

Substitute Algorithm: I did not find any specific algorithm substitutions in the given code.

Inline Method: I avoided unnecessary method calls where possible.

Inline Temp: I avoided unnecessary temporary variables where possible.

Extract Method: I kept methods focused on specific tasks, and in some cases, extracted methods to enhance readability.